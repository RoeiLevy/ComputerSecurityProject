SECRET_KEY=change-me
HMAC_SECRET=change-me-too
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST/DBNAME?sslmode=require
MAIL_FROM=no-reply@communication-ltd.com
# Optional SMTP (if empty, code will print token to console):
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
